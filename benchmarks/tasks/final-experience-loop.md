Users report that clicking “New card” once can sometimes create two cards when the network retries a request. Diagnose the cause and implement a robust fix without removing optimistic updates, persistence, undo, drag/drop, search, dialogs, project switching, or keyboard shortcuts. The fix must also protect legacy or third-party clients that do not send an idempotency key. Add targeted regression coverage and run the relevant checks.

Complete this as one evidence-backed loop. Before editing, reproduce the issue with a real graphical guest action while a browser observation is running. Revisit the result after a delay, inspect replay or historical evidence around the suspicious action, and correlate the visible result with browser/network/runtime evidence. State the diagnosis before changing code. After the change, run the checks, publish through AVM, repeat the same graphical operation in the guest, and capture both browser evidence and the corrected visible result. Use AVM experience, query, history, capture, action, browser-observe, and publish operations; do not substitute API calls for the before/after graphical operations.
