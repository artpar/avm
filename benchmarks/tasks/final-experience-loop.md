Users report that clicking “New card” once can sometimes create two cards when the network retries a request. Diagnose the cause and implement a robust fix without removing optimistic updates, persistence, undo, drag/drop, search, dialogs, project switching, or keyboard shortcuts. The fix must also protect legacy or third-party clients that do not send an idempotency key. Add targeted regression coverage and run the relevant checks.

Complete this as one evidence-backed loop. Before editing, call browser-observe in `start` mode with `durationMs: 90000`, immediately reproduce the issue with real graphical guest actions, then call it in `finish` mode with the returned observation ID so the network recorder overlaps the action. Do not call capture, history, replay, or query between browser-observe start and finish; acquire those after finish so slow evidence calls cannot exhaust the recording window before the input. The product's `N` keyboard shortcut (Linux input keycode 49) is a valid real QEMU input path for opening New card when unverified pointer geometry is uncertain. From the focused title field, four Tab presses (keycode 15) focus Save; Enter (keycode 28) then activates it. Revisit the result after a delay, inspect replay or historical evidence around the suspicious action, and correlate the visible result with browser/network/runtime evidence. Use a canonical timeline event UUID returned by history—not an action ID or an invented timestamp—as the anchor for at least one successful AVM query. State the diagnosis before changing code. After the change, run the checks, publish through AVM, repeat the same graphical operation inside another `start`/`finish` browser observation using the same immediate-input ordering, and then capture the corrected visible result. Use AVM experience, query, history, capture, action, browser-observe, and publish operations; do not substitute API calls for the before/after graphical operations.
